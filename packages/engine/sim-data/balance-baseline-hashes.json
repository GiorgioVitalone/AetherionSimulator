{
  "smoke": {
    "runHash": "08504f2475115406",
    "note": "heuristic all-pairs GPP=8 under ruleset-v1 + real official starter decks (deck-loader's committed fixture, sim-data/aetherion-decks.json — hermetic, no Docker/Postgres); bump ONLY with a cited full-panel ledger run id in the commit message",
    "provenance": "repinned 2026-07-16 (round-5 T1 fix): shield (EC-003 on_would_take_damage) valuation corrected from first-instance-per-turn (falsely claimed locked in v1) to per-instance (SHIELD_INSTANCES_PER_TURN=2, v1's actual unconfigured default — shieldFirstInstanceOnly is absent from sim-data/ruleset-v1.json) — value-pilot deploy scoring for shield-bearing cards changed under locked rules; rollout verdict tier unaffected. Prior pin (2026-07-15, S1/S2 pricer recalibration) superseded."
  }
}
